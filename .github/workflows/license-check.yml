name: license-check
on: [push]
jobs:
  check-non-open-source-licenses:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v2
      - uses: actions/setup-node@v1
      - run: npm install js-green-licenses
      - run: yarn jsgl --local



